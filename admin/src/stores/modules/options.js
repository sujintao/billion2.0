const state = {
    seloptions:null
}

const getters = {}
const actions = {}

const mutations = {
    setseloptions: (state, obj) => {
        state.seloptions = obj
    }
}

export default {
    state,
    getters,
    actions,
    mutations
}